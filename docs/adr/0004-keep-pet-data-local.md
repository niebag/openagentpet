# Keep Pet data local

The plugin and Companion app send no session data off the machine in v1. The Companion receives only an opaque local session identifier and display state; it stores no transcripts and includes no analytics or crash reporting. Explicit npm installation and confirmed updates, plus a user-triggered npm version check, are the only planned network operations.
